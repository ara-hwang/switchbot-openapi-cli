{
  "mcpServers": {
    "switchbot": {
      "command": "npx",
      "args": ["-y", "@switchbot/openapi-cli", "mcp", "serve"],
      "env": {
        "SWITCHBOT_TOKEN": "${SWITCHBOT_TOKEN}",
        "SWITCHBOT_SECRET": "${SWITCHBOT_SECRET}"
      }
    }
  }
}
